---
title: "日志规范与成本治理：日志平台的第二课"
date: 2022-05-10T09:00:00+08:00
lastmod: 2022-05-10T09:00:00+08:00
draft: false
author: IYABAO
tags:
  - 日志
  - ELK
  - 成本优化
categories:
  - 技术
summary: "日志不是越多越好：每天 5TB 日志，存储成本惊人。日志规范（级别/字段/内容）、采样、归档、成本预算——日志治理是 ELK 之后必须补的第二课。"
---

# 日志规范与成本治理：日志平台的第二课

搭好 ELK 只是第一课。第二课是：**日志量失控了怎么办**。我见过团队每天产生 5TB 日志、月度存储成本 20 万——其中 80% 是没人看的。日志治理 = 规范（写什么）+ 成本（存多少）+ 可用（找得到）。

## 一、先看成本是怎么爆的

```
日志成本的构成：
  存储（热数据 × 副本 × 保留天数）
  传输（网络带宽）
  查询（ES 节点规格）

爆量根因：
  1. 无差别打日志（每行代码都 debug）
  2. 大对象直接打印（整个响应体 50KB）
  3. 循环里打日志（1 万次循环 1 万条）
  4. 保留期过长（全量 180 天）
  5. 冗余字段（每次打 20 个字段，用到的 5 个）
```

## 二、日志规范（写什么）

### 级别规范

| 级别 | 用在哪 | 示例 |
|---|---|---|
| DEBUG | 本地调试 | 参数细节 |
| INFO | 关键业务事件 | 下单成功、用户注册 |
| WARN | 可恢复异常 | 重试、限流触发 |
| ERROR | 需要人工关注 | 调用失败、数据不一致 |

**规范**：

```
✅ 生产默认 INFO（DEBUG 不落盘，除非排查）
✅ ERROR 必须带上下文（哪个用户、哪个订单、哪个请求）
✅ 错误日志要有"可行动性"（看了知道怎么处理）
❌ 别打"Error occurred"这种无信息量日志
```

### 字段规范（结构化）

```json
{
  "ts": "2026-05-10T10:00:00",
  "level": "ERROR",
  "service": "delivery-service",
  "trace_id": "abc123",
  "user_id": "u-123",
  "code": "ORDER_PAY_TIMEOUT",
  "msg": "订单支付超时",
  "duration_ms": 2300
}
```

**统一字段**：时间/级别/服务/trace_id/业务 ID/错误码/消息——**没有统一字段，检索靠猜**。

## 三、成本治理（存多少）

### 分层存储

```
热数据（近 7 天）：SSD，完整字段，秒级查询
温数据（7-30 天）：机械盘/冷存储，降采样
冷数据（30-180 天）：对象存储（归档），查用再拉

→ 成本：热 70% + 温 25% + 冷 5%
```

### 采样与裁剪

```
✅ 高流量接口（健康检查、心跳）：采样 10% 或直接不打
✅ 大字段裁剪（响应体只保留 status + size，不存 body）
✅ 敏感字段脱敏（手机号/密码/Token 打码）
✅ 重复日志聚合（同错误 1 分钟 N 次 → 合并成 1 条 + count）
```

### 预算制

```
按服务/按团队分配日志预算（GB/天）
超预算告警 + 自动降级（ERROR 之外采样）
→ 让日志量"可预期"，而不是月底看账单
```

## 四、日志平台的高阶能力

```
✅ 检索规范：trace_id 全链路检索（一条请求的所有日志）
✅ 告警：ERROR 率突变 / 特定错误码出现 → 告警
✅ 分析：按接口/错误码聚合（TopN 错误排行）
✅ 归档恢复：冷数据查询通道（不能查 = 白存）
```

## 五、踩坑记录

1. **循环打日志**：`for i := range 10000 { log.Debug(i) }` → 日志爆炸 → 循环里只打"最终结果"，或限频（每 100 次一条）
2. **打印敏感数据**：日志里出现用户手机号 → 合规事故 → 统一脱敏过滤器
3. **ERROR 不带上下文**：只报"调用失败"，不知道哪个订单哪个用户 → 规范字段 + 必填校验
4. **保留期一刀切**：全量 180 天 → 存储爆 → 分层：ERROR 全保留，INFO 降采样

## 六、日志治理的投入产出

```
治理前：5TB/天，20 万/月，查一条日志要 5 分钟
治理后：1.2TB/天（-76%），成本 -70%，
       关键问题 30 秒内定位（trace_id 一键查全链路）
```

## 总结

日志治理的核心认知：

1. **规范先行**：级别、字段、内容三个规范，让日志"可检索可行动"
2. **成本分层**：热/温/冷三层存储 + 采样裁剪 + 预算制
3. **日志是给排查用的，不是给"安心"用的**：没人看的日志 = 浪费
4. **治理指标**：检索效率 + 存储成本 + 告警准确率

**日志的第一课是"怎么收"，第二课是"怎么少收"。** 会写日志是本能，会治理日志才是工程能力。
